<nav>
    <div class="nav-bar">
        <i class='bx bx-menu sidebarOpen'></i>
        <span class="logo navLogo" style="margin-left:-100px;">
    <a class="navbar-brand" href="https://www.ultramaille.com/" target="_blank" style="color:#18306D;">
        Ultramaille S.A
    </a>
</span>
        <div class="menu">
            <ul class="nav-links">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li><a href="{{ path('app_home') }}">Accueil</a></li>
                    <li><a href="{{ path('app_client_alls') }}">Clients</a></li>
                {% endif %}                    
            </ul>
        </div>

        <div class="darkLight-searchBox">
            <div class="searchBox">
                <div class="dropdown">
                    <p class="dropbtn btn shadow" style="border-radius: 5px; background:#18306D; color:#fff; padding: 5px 10px; border: none; margin-top: 15px;">
                        <i class="fa-solid fa-user-tie fa-lg" style="font-size: 20px;"></i>
                    </p>
                    <div class="dropdown-content">
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <a href="{{ path('app_logout') }}" class="dropdown-item">
                                <em class="fa fa-sign-out"></em>
                                Déconnexion
                            </a>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="dropdown-item"><em class="fa fa-sign-in"></em> Connexion</a>
                            <a href="{{ path('app_register') }}" class="dropdown-item">
                                <em class="fa fa-user-plus"></em>
                                Inscription
                            </a>
                        {% endif %}   
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>
